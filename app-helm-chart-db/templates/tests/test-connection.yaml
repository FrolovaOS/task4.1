apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-helm-chart-db.fullname" . }}-test-connection"
  labels:
{{ include "app-helm-chart-db.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
{{/*spec:*/}}
{{/*  containers:*/}}
{{/*    - name: wget*/}}
{{/*      image: busybox*/}}
{{/*      command: ['wget']*/}}
{{/*      args:  ['{{ include "app-helm-chart-db.fullname" . }}:{{ .Values.spec.port }}']*/}}
{{/*  restartPolicy: Never*/}}
